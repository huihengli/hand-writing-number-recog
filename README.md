'torch手写识别.py'是直接调用MNIST的数据进行识别的，主要演示功能完整性
'torch现场识别.py'有加载PIL和tkinter库，有手写演示工能
识别准确度算挺高的，应该能有97%左右，但是有些手写数字还是识别不太好，尤其是在'torch现场识别.py'经常出现识别错误的情况，多试几次就好了
上述两个程序均需要现场训练，但是saved_model文件夹有训练好的模型应该是可以直接加载来使用的，但是我没写这个功能，忘记了，很好写的，就是把训练的对应模块换成load_model之类的传给对应的类就行
